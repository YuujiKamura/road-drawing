# Deprecated: Replaced by deploy.yml (Flutter Web + WASM)
# This file is intentionally empty to avoid duplicate deployments.
# See deploy.yml for the active GitHub Pages workflow.

name: "[Deprecated] Pages (use deploy.yml)"

on:
  workflow_dispatch: # Manual only, never auto-triggers

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use deploy.yml instead."
